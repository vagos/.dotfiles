set previewer ~/.config/lf/preview

set ratios 1:2
set info size:time

cmd bg ${{
    chbg $f
}}

map D delete
map . set hidden!
map <enter> open
map <c-o> !dragon-drop "$f"
map <c-e> !nv-remote "$f"
